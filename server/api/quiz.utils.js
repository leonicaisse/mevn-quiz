const mapPostData = (data) => {
  // TODO: Format posted data before submitting to database
  return validateData(data);
};

const validateData = (data) => {
  // TODO: Validate posted data before submitting to database
  return data;
};

module.exports = {
  mapPostData,
};
